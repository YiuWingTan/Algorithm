#include<iostream>
#include<vector>

using namespace std;

struct TreeNode {
	int val;
	TreeNode *left;
	TreeNode *right;
	TreeNode(int x) : val(x), left(NULL), right(NULL) {}
	
};

bool find(TreeNode*node,TreeNode* target,int par,int &val,int k,vector<int>&result)
{
	
}

vector<int> distanceK(TreeNode* root, TreeNode* target, int K) {

	if (root == nullptr) return{};
	vector<int> result;

	
	

	return result;
}

int main()
{


	return 0;
}


